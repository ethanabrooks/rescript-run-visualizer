table:
  name: parameter_choices
  schema: public
object_relationships:
- name: sweep
  using:
    foreign_key_constraint_on: sweep_id
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - sweep_id
    - Key
    - choice
  role: public
select_permissions:
- permission:
    columns:
    - sweep_id
    - Key
    - choice
    filter: {}
  role: public
